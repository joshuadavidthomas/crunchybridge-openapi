observed_on: 2026-09-04
imported_release: v0.1.0
consumer: Executor
scope: Exact owner-approved work-account test team; team and account identifiers omitted.
credentials: Configured in Executor; no bearer secret appeared in tool inputs or emitted results.
import:
  catalog_operations: 84
  descriptors_inspected: 84
  pagination_complete: true
  duplicate_names: []
  missing_descriptors: []
  unresolved_ref_text_in_inputs: []
  representative_input_checks:
    - getTeam preserves required team_id.
    - listClusters preserves optional team_id, limit, cursor, ordering, and nullable team filter.
    - createCluster preserves required body, name, plan_id, and team_id; arrays and nullable fields remain visible.
    - createQuery preserves required body and SQL, repeatable result rows, and nullable result fields.
    - upsertPostgresRole preserves required path identifiers and optional request body.
  limits: >-
    Descriptor checks inspect TypeScript text, not runtime input validation.
    EID patterns and cross-field JSON Schema constraints are not expressible in
    those types. Custom risk extensions are not surfaced by the compact tool
    descriptors; their enforcement is unverified.
read_observations:
  - operationId: getTeam
    status: 200
    result: Approved team matched; billing_address and automatic_sso_join returned null.
  - operationId: listClusters
    status: 200
    result: Empty list for the approved team; has_more was false and next_cursor was null.
  - operationId: listNetworks
    status: 200
    result: Empty list for the approved team.
  - operationId: listTeamMembers
    status: 200
    result: Active member with a nested account and empty access_groups array.
  - operationId: getTeamMember
    status: 200
    result: Looked up an account returned by the approved team's member list; team scope matched.
  - operationId: listEvents
    status: 200
    result: Team-scoped event with previous_properties null; non-null cursor observed.
  - operationId: getEvent
    status: 200
    result: Looked up an event returned by the approved team's event list; team scope matched.
  - operationId: getTeamCertificate
    status: 200
    contentType: application/pem-certificate-chain
    bodyType: string
    begins_with_pem_certificate_marker: true
    result: Certificate bytes not retained; observed media type and string handling only, not cryptographic validation.
fixtures: ../tests/contract/fixtures.yaml
fixture_operations: 7
fixture_limits: >-
  The JSON fixtures preserve observed field presence, nulls, and structure with
  synthetic sensitive values. Empty cluster and network lists do not exercise
  their item schemas. No pending-invitation or SSO-enabled team fixture was observed.
safety:
  mutation_calls: 0
  resources_created: 0
  approval_enforcement: not_tested
  policy_denial_calls: 0
  notes: No destructive operation, including destroyAccount, was invoked to probe approval policy.
